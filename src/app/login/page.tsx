'use client'
import LoginForm from '@/Components/LoginForm'
import React from 'react'

type Props = {}

const page = (props: Props) => {
    return (
        <div><LoginForm /></div>
    )
}

export default page